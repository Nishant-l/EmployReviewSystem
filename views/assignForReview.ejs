<link rel="stylesheet" href="/css/asignForReview.css">
<form action="/admin/assignForReview-form" method="post" id="assignForm" >
    <div id="assignContainer">
        <article class="panel is-primary" id="left">
            <p class="panel-heading" >
                Select member who will review
            </p>
            <div>
                <%for(e of employe){%>
                    <div class="panel-block is-active" id= "<%=e._id%>" >
                        <label class="radio">
                            👤 <%= e.name %>
                            <input type="radio" name="reveiwer" value="<%=e._id%>">
                        </label>
                    </div>
                <%}%>
            </div>
        </article>
        <article class="panel is-danger" id="right">
            <p class="panel-heading">
                Select member to be reviewrd
            </p>
            <div>
                <%for(e of employe){%>
                    <div class="panel-block is-active" >
                        <label class="checkbox" id= "right<%=e._id%>">
                            👤 <%= e.name %>
                            <input  type="checkbox" name="toBeReviewed" value="<%=e._id%>" class="<%=e._id%>">
                        </label>
                    </div>
                <%}%>
            </div>
        </article>
    </div>
    <input id="subBut" class="button is-info" type="submit">
</form>
<script src="/demo.js"></script>


<!-- <script> // ToDo: for making the check box inactive when the employ is alredy assigned for review or reviewed by the peer employee
    const aa = document.getElementById('left');
    const hasClicked = new Set();
    aa.addEventListener('click',(e)=>{
        const bb = document.getElementById('right');
        const cc = document.getElementById('right'+e.target.value);
        for(i of hasClicked){
            if(i!=null){
                i.firstElementChild.Attribute('enabled')
                i.setAttribute('disabled',false);
            }
        }
        hasClicked.add(cc);
        console.log(hasClicked);
        console.log(cc.firstElementChild)
        cc.firstElementChild.setAttribute('disabled',true)
        cc.setAttribute('disabled',true);
    });
</script>  -->
<!-- <% const regex = new RegExp("[e]") %> //ToDo: implement the search functionality

<%= employe.filter(e => regex.test(e.name)) %> -->

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
